# Data

This directory contains the MNIST dataset files used by Part 1.

The `.mat` dataset files are not tracked in Git because they are external data artifacts.

Place the required MNIST `.mat` files in this directory before running the Part 1 experiments.
